// import "@testing-library/jest-dom";
// import Progress from "../components/Tester/Test/Progressbar/Progress";
// import { render, fireEvent, screen, container  } from "@testing-library/react";

// it("renders correctly", () => {
//   const { view } = render(<Progress ProgressValue={1} />);

//   expect(view).not.toBeNull();
// });

// it("renders correctly2", () => {
//   const { debug, getAllByDisplayValue, container  } = render(<Progress ProgressValue={1} />);
//   const progress = screen.getByRole('progress', { hidden: true });

// });
